apiVersion: v1
kind: Service
metadata:
  name: certfleet
  namespace: certfleet
  labels:
    app.kubernetes.io/name: certfleet
    app.kubernetes.io/component: hub
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: certfleet
    app.kubernetes.io/component: hub
  ports:
    - name: http
      port: 80
      targetPort: http
